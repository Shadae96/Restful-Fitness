<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Registry</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/register.css">
</head>

<body>
  <header class="display-flex justify-space-between align-center p-2">

    <!-- Only display the logout button if the user is logged in -->
    {{#if logged_in}}

    <h1>Welcome Back!</h1>
    <nav>
      <button class="btn" id="newWorkout">new workout</button>
      <button class="btn" id="myWorkouts">my workouts</button>
      <button class="btn" id="community">community</button>
      <button class="btn" id="logout">logout</button>
      <button class="btn" id="blog"> blog</button>
    </nav>
    {{/if}}
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>
  <script src="/public/js/register.js"></script>

  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html>